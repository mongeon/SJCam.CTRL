<Page
    x:Class="SJCamCTRL.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SJCamCTRL"
    xmlns:controls="using:SJCamCTRL.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources >

    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="SJCam CTRL"/>
        <Button Grid.Row="1" Background="Red" HorizontalAlignment="Center" Command="{Binding RefreshCommand}" Width="500" Height="200" />
        <controls:InformationPanel Grid.Row="2" DataContext="{Binding Camera}" />

    </Grid>
</Page>